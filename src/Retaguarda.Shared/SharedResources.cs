namespace Retaguarda.Shared;

// Tipo marcador (não tem código). Serve como T em IStringLocalizer<SharedResources>
// quando Web/Api precisarem de chaves compartilhadas entre vários componentes.
// As entradas reais vivem em Resources/SharedResources.<culture>.resx neste assembly.
public sealed class SharedResources
{
}
